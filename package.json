{
  "name": "zen-connect",
  "version": "1.0.0",
  "description": "A lightweight node server for zen, providing connection to OSC, custom samples and more.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "concurrently \"node samplesToJSON.js && serve -l 5000\" \"node osc-bridge.js\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.1",
    "directory-tree": "^3.5.1",
    "fs": "^0.0.1-security",
    "serve": "^14.2.1",
    "node-osc": "^1.2.0",
		"socket.io": "^1.7.4"
  }
}
